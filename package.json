{
  "name": "pan-india-logistics-platform",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n BACKEND,ADMIN,ENTERPRISE -c blue,green,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix web-portal-admin\" \"npm run dev --prefix web-portal-enterprise\"",
    "install:all": "npm install --prefix backend && npm install --prefix web-portal-admin && npm install --prefix web-portal-enterprise",
    "seed": "npm run seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
